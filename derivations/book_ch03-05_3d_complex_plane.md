# Book Chapters 3–7 — 3D Complex Plane: Formal Map

**Reader one-pager:** `book/00_Front_Matter.js` → section *The 3D Complex Plane in One Picture* (built to `book/output/00_Front_Matter.docx`).

**Scope:** Part II (this file) = **mathematical construction** (DERIVED/PROVEN). Parts III–VI = **physical readings** (MODEL/ANCHORED). The lattice formula and 32-chamber closure are the core advance; particle narratives interpret them.

**Pipeline build (bit-by-bit):** `derivations/pipeline_3d_complex_plane_plan.md` — accuracy/speed/compression plan with math, cons, and gate tests **before** each code step.

**Canonical name:** **3D complex plane** Ψ=(z,ζ). Generated by the **lattice formula** on anchor chains.  
**Not** the π lattice (Ch 1–2, `pi/`). **Deprecated names:** prime lattice, infinity lattice, φ-prime lattice — see repo `ONTOLOGY.md`.

Executable source: `aethos_complex_plane.py`, `aethos_complex_spring.py`, `aethos_complex_rotation.py`, `aethos_intersection_nodes.py`. Hilbert layering: `derivations/hilbert_space.md`. Tests: `tests/test_complex_plane.py`.

## Ontology — construction vs coordinate chart (Ch 3 §3.1)

| Layer | Statement | Tag |
|-------|-----------|-----|
| **PRIMITIVE** | Rail n, chain A, segment FSM, VA1–VA4, wings 1–8, meet quotient | **DERIVED** |
| **READOUT** | canon_on_chain → wing_transform → z=X+iY, ζ=Z | **DERIVED** |
| **OBJECT** | 3D complex plane = trajectory space + (z,ζ) readout | **MODEL** |
| λ_C floor | Ch 1 π-lattice cells set scale; plane is separate construction | **ANCHORED** + **MODEL** |
| ℝ³ plot | Camera on one (b,w,n), not prior container | **MODEL** |
| ℂ from ops | `i_act = R_x∘S` on (X,Y); conj from `-R_x` readout | **PROVEN** (`aethos_spring_complex.py`) |
| π functor | depth→n, bits→wing, layer0↔π/4, k=0↔i | **PARTIAL** (`aethos_pi_bridge.py`); full (A,b,w,n) **OPEN** |
| ICN | prime composite C; normalize → chain → Ψ; not z=X+iY | **MODEL** (`ONTOLOGY.md` §B.5) |
| Imaginary vector | spring + branch fan b; phase engine (main stick) | **MODEL** (§B.6) |
| Complex vector | 2-way swap-meet witness path | **DERIVED** (§B.6) |
| Complex number | 3-way Ψ=(z,ζ) node; not textbook a+bi alone | **MODEL** (§B.6) |

### Formula pipeline (PROVEN at tested chains)

1. `s = segment_index_chain(A, n)`
2. `ζ = z_accumulator(A, n, s)` — interior lock
3. `(X,Y,Z) = canon_on_chain(b, A, n)` — VA1–VA4 in `aethos_sequences.py`
4. `(X_w,Y_w,Z_w) = apply_vector(..., wing)` — `aethos_lattice.py`
5. `Ψ = (complex(X_w,Y_w), Z_w)` — `aethos_complex_plane.py`

**Example:** PRIMES k=5, n=10 → VA1 wing 1 → z=23+10i, ζ=39 (**PROVEN** `test_complex_plane.py`).

---

## Chapter 3 — Ψ = (z, ζ)

| Object | Definition | Tag |
|--------|------------|-----|
| State | `Ψ = (z, ζ)`, `z = X+iY`, `ζ = Z` | **DERIVED** (code) |
| Address | `α = (A, b, w, n)` | **DERIVED** |
| Layer 0 | `z_0(n) = n(1+i)`, `ζ_0 = n` | **PROVEN** (tests) |
| Segment FSM | `canon_on_chain`, `derive_va1_closed_form` | **DERIVED** |
| π link | `(1−A, ±B)` limit vs spring readout | **MODEL** partial |

---

## Chapter 4 — 32 chambers

| Count | Structure | Tag |
|-------|-----------|-----|
| 4 | branches VA1–VA4, distinct `z` at fixed (A,w,n) | **PROVEN** (test_four_branches) |
| 8 | wings, Klein-4 on `z` × `ζ` flip | **DERIVED** (rotation module) |
| 32 | `lattice_id = 1 + b×8 + (w−1)` | **DERIVED** |
| \|z\|² | Born/Malus envelope | **ANCHORED** / **PARTIAL** O1-1 |

WH/WS/BH/BS (Ch 8) ≠ VA branches — related four-outcome layers, not proven 1:1 map.

---

## Chapter 5 — Meets and Hilbert stack

| Mechanism | Code | Tag |
|-----------|------|-----|
| `swap_meet(a,p)` | equal coords both ways | **PROVEN** (integral chains) |
| `triple_equalization` | missing-variable rule | **PROVEN** (3,5,7) |
| Meet quotient | same `(X,Y,ζ)`, distinct path labels | **MODEL** entanglement |
| Interior ζ lock | bulk subspace | **MODEL** sea bulk |

**PROVEN:** wing operators define `i` and conjugation on spring readout (`tests/test_spring_complex.py`).

**PARTIAL:** `aethos_pi_bridge.py` — layer-0 π/4, k=0 unit i, dyadic vertices, demo maps depth→n and bits→wing.

**OPEN:** full functor π-address ↔ `(A,b,w,n)` for all chains.

---

## Chapter 6 — 3D complex plane (unbounded extension)

| Capability | Mechanism | Tag |
|------------|-----------|-----|
| True 3D | `Ψ ∈ ℂ × ℝ`, not ℝ³ alone | **DERIVED** |
| n → ∞ | transgressor rail unbounded | **DERIVED** |
| k → ∞ | `make_chain(kind, k)` extend countably | **DERIVED** |
| depth → ∞ | `OriginTree`, 3 children per meet | **DERIVED** |
| Species | `⊕_{kind} H_species` | **DERIVED** |
| Separable Hilbert | finite labels, countably many | **MODEL** completion |
| Window count | `estimate_hilbert_tower(...)` | **DERIVED** demo |

**Construction:** kind → chain A → (b,w,n) → `wing_transform` → Ψ; meets spawn origins; extend without bound.

---

## Chapter 7 — SequenceKind worked examples

| Species | k=5 chain A | n=10 (VA1, w=1) X,Y,ζ | \|z\|² | Tag |
|---------|-------------|-------------------------|--------|-----|
| PRIMES | (3,5,7,11,13) | 23, 10, 39 | 629 | **PROVEN** (test_different_species) |
| EVENS | (2,4,6,8,10) | 20, 10, 40 | 500 | **PROVEN** |
| POWERS_2 | (2,4,8,16,32) | 42, 10, 62 | 1864 | **PROVEN** |
| SQUARES | (1,4,9,16,25) | 35, 10, 55 | 1325 | **PROVEN** |
| TRIPLES | (3,6,9,12,15) | 25, 10, 45 | 725 | **PROVEN** |

| Mechanism | Code | Tag |
|-----------|------|-----|
| Species selector | `make_chain(SequenceKind, k)` | **DERIVED** |
| Segment schedule | `segment_at`, interior ζ lock | **DERIVED** |
| FIBONACCI | duplicate 1,1 fails `normalize_chain` | **OPEN** — use CUSTOM deduped chain |

**Book tables 7.1–7.4** reproduce `canon_on_chain` / `wing_transform` at matched n; changing kind at fixed n changes Ψ (**PROVEN**).
