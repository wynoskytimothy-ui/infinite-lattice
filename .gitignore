# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
dist/
build/
.venv/
venv/
env/

# Test / tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Secrets / local config
.env
.env.*
*.pem

# Large or machine-local data (not part of source)
beir_datasets/
benchmark_data/
*.brain.json
brains/

# Optional heavy outputs
ocean_trace_demo.csv

# Regenerable data caches / downloaded datasets (not source).
# (Comments must be on their own line — git ignores no trailing comments.)
# marco_pool.pkl regenerates via marco_lab.load_pool(); text8.zip is a download;
# *.npz are bearing datasets; bearing_*.json are monitor run outputs.
marco_pool.pkl
text8.zip
text8
*.npz
bearing_*.json
wiki_cache.json
_viz_series.txt

# Node + run logs (never source)
node_modules/
logs/
